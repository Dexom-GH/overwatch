# Claude Code machine-local settings
.claude/settings.local.json

# OS / editor cruft
Thumbs.db
.DS_Store

# Secrets / local env
.env

# Model artifacts (rebuilt on device; see models/README.md)
models/*
!models/README.md
*.onnx
*.engine

# Detector assets staged on-device by scripts/target/57_stage_detector_assets.sh
# (#97): the DeepStream-Yolo parser .so under models/, and a config-dir symlink
# (configs/models -> repo-root models/) so nvinfer resolves its config-relative
# asset paths onto the real engine + parser. Not committed.
/src/overwatch/inference/deepstream/configs/models

# Captured data
data/

# Local-only benchmark notes (see model-convert-benchmark workflow)
docs/BENCHMARKS.local.md

# Node / frontend toolchain (operator-console SPA, #124) — built in CI, never
# committed. dist/ below also covers the SPA's web/dist build output.
node_modules/

# Python build / cache
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.venv/
venv/

# Training datasets (downloaded; not committed) — #77
datasets/

# Ultralytics training outputs / weights (#77)
runs/
*.pt
