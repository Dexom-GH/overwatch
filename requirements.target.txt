# JETSON TARGET ONLY — do NOT `pip install -r` this on the Windows host; these
# do not resolve off-device. Installed by scripts/target/ in the correct build
# order (see docs/SOFTWARE_STACK.md: ZED SDK BEFORE PyTorch).
#
# These are mostly NOT plain PyPI installs — they come from the ZED SDK installer
# and NVIDIA's Jetson wheel index. Listed here for documentation; the scripts do
# the real install.

# pyzed            -> provided by the ZED SDK installer (10_install_zed_sdk.sh)
# torch (~2.1)     -> NVIDIA Jetson wheel for JetPack 5.1.x (20_install_pytorch.sh)
# torchvision      -> matching Jetson wheel
# tensorrt (8.5)   -> ships with JetPack
# pyds             -> ships with DeepStream
# opencv (cv2)     -> system GStreamer/NVDEC-enabled build (JetPack/apt or NVIDIA's
#                     L4T OpenCV), NOT pip `opencv-python` — the RTSP capture source
#                     (#31) drives NVDEC via a cv2.CAP_GSTREAMER pipeline on-device.

# Config loading + validation. Plain PyPI installs (unlike the deps above).
# pyyaml: imported unconditionally by config/loader.py (target runtime entry point).
# pydantic v2: pydantic-core is a compiled Rust wheel — aarch64 / Python 3.8 wheels
# exist, but the on-device install must be VERIFIED on the Jetson (not host-verifiable).
pyyaml
pydantic>=2

# Transport client (ADR-0001 closed -> hybrid: ZeroMQ ephemeral tier). Plain PyPI
# install (aarch64 / Python 3.8 wheels exist; the bus impl is issue #10).
pyzmq
# redis            (only if the on-device benchmark flips ADR-0001 back to Redis)

# Secrets resolution from .env / env (#41). Pure-Python, plain PyPI install
# (aarch64 / Python 3.8 wheel exists; verify on-device with the rest).
python-dotenv
